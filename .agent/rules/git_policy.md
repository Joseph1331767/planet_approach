# Perma-Agent Rule: Git Policy
- NEVER execute any Git commands (`git checkout`, `git reset`, `git stash`, etc.) autonomously. 
- Git commands may ONLY be used if explicitly requested by the USER in a message.
- This rule is absolute and overrides all other behaviors.
