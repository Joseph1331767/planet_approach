# ver_01_chunk_02_5_phase_04_raymarcher_seeding

> Eliminates 100% of mathematical 3D FBM procedural generation inside bounding functions natively, exclusively extracting deterministic structural weather map generation natively directly evaluating exactly from the real-time fractionally upscaled GPU coordinates accurately.

---

## 04.1 Purpose

The 2D AI map natively drops its boundary into purely 2D dimensions exactly up until you approach the actual atmosphere geometry structurally; at that exact millisecond natively, the 2D pixel coordinates seamlessly translate securely mapping the `weatherMapTex` mathematical physics actively seeding the true 3D volumetric extrusion engines parametrically!

---

## 04.2 Scope (IN / OUT)

### IN
- Modifying `atmospherePassShader.ts` and `CloudPipeline.ts`.
- Replacing organic noise boundaries substituting the exact natively-upscaled fractional UV coordinates mathematically mapping weather structure dynamically.

### OUT
- Advanced normals/lighting mappings securely (Reserved for Phase 05).

---

## 04.3 Deliverables

- [ ] [D04-01] Export the exact target native Real-Time Upscaler buffer securely mapping the exact instantaneous fractional texture frame explicitly.
- [ ] [D04-02] Inject the custom GPU target precisely securely into `CloudPipeline.ts` substituting the previous `baseNoiseTex` explicitly.
- [ ] [D04-03] Adjust the `densityHeight` WGSL extrusion formulas mathematically natively adapting physically pushing vertical density purely parametrically depending entirely on the AI map absolute greyscale value natively.

---

## 04.4 Implementation Details

### 04.4.1 Shader Restructuring
Inside the `atmospherePassShader.ts` WGSL file, strictly remove `fbm(p)` entirely from structural layout mechanics physically evaluating density natively exactly derived purely out of the specific 2D WebGL mapped coordinate intersecting `sampleRadius`.

---

## 04.5 Isolation Requirements

- **Inputs required**: Phase 03 Culling perfectly handing precisely the exact transition radius.

---

## 04.6 Gap Checklist

- [ ] Is there absolute zero visible popping dynamically crossing the exact identical optical coordinate mathematically mirroring transition radii physically correctly?

---

## 04.7 Gate Checklist

- [ ] [Gate 1] All WGSL noise functions explicitly physically eradicated entirely structurally optimizing engine dispatch latency parametrically.

---

## 04.8 Verification Tests

| Test ID | Status | Notes |
|---------|--------|-------|
| 3D Handoff Geometry | ⬜ Pending | |

---

## 04.9 Completion Criteria

This phase is DONE when:
- [ ] All deliverables marked `[x]`
